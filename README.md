# action-xcodebuild-destination-apple
Returns the xcodebuild destination for the input platform.

Expects any desired simulator to already available on the machine.

The output id for reference is `destination`.
